#include<iostream>
#include<cmath>

int main(){
    double x = 10.49;
    double y = 20;
    double z;

    // z = std::min(x, y);
    // z = std::max(x, y);
    // z = pow(2, 3);
    // z = ceil(x);
    // z = floor(x);
    // z = round(x);
    // z = sqrt(x);
    z = abs(-2);

    std::cout << z;
    return 0;
}